# ~/.bashrc_config
# This file contains custom configurations and is sourced by ~/.bashrc

# --- Add ~/bin to the PATH ---
# This makes all the custom scripts in the ~/bin directory (like update_system)
# available as commands in the terminal.
export PATH="$HOME/bin:$PATH"

# --- Welcome Message ---
# This function displays a cool welcome message when a new terminal is opened.
show_welcome_message() {
    if command -v figlet &> /dev/null; then
        figlet -f slant "My Linux Setup" | lolcat
    else
        echo "Welcome to My Linux Setup"
    fi
    echo "-------------------------------------------------"
    echo "All custom scripts and aliases are now loaded."
    echo "Try running 'update_system' or 'll'."
    echo "-------------------------------------------------"
}

# Uncomment the line below to show the welcome message on every new terminal
# show_welcome_message

# --- Alias Definitions ---
# Source the separate aliases file if it exists.
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

# --- Oh My Posh Configuration ---
# This line initializes the custom prompt.
eval "$(oh-my-posh init bash --config ~/.poshthemes/bubbles.omp.json)"

# --- Zoxide (smarter cd) Initialization ---
# This is required to make the 'z' command work.
eval "$(zoxide init bash)"

# --- Eza (modern ls) Aliases ---
# These aliases make 'ls' and 'll' use the new, better 'eza' command.
alias ls='eza --icons'
alias ll='eza -l --icons'
alias la='eza -la --icons'
